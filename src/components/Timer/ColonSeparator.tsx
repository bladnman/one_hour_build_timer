/**
 * Colon separator between time segments
 * Animates with a subtle throbbing effect when timer is running
 */
export function ColonSeparator() {
  return (
    <span className="colon-separator" aria-hidden="true">
      :
    </span>
  );
}
