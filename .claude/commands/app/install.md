Install the Countdown Timer app to /Applications.

Run the install script:
```bash
./install.sh
```

This will:
1. Build the app if no build exists
2. Copy the app to /Applications
3. Replace any existing installation
